# =====================================================================
# requirements.proposed.txt  (提案版 / 既存 requirements.txt は上書きしない)
# バージョンは現在アクティブな llmagent venv の `pip freeze` に基づく (Python 3.10)
# 実際の import スキャン結果と突き合わせて整理済み。
# 導入前に本ファイルをレビューし、問題なければ requirements.txt へ反映してください。
# =====================================================================

# --- LangChain 本体 / コア ---
langchain==1.1.3
langchain-core==1.2.16

# --- LLM プロバイダ別 LangChain 統合 (実コードで使用 / 旧 requirements に不足していた) ---
langchain-openai==1.1.3          # ChatOpenAI (judge_llm 等)
langchain-ollama==1.0.1          # ChatOllama (base / ローカルLLM)
langchain-anthropic==1.3.4       # ChatAnthropic (claude 版)
langchain-google-genai==4.1.2    # ChatGoogleGenerativeAI (gemini / gemma 版)

# --- 数値/ML (auto_shapley_router_gpt.py で使用 / 旧 requirements に不足) ---
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2

# --- Google Calendar API ---
google-api-python-client==2.187.0
google-auth==2.45.0
google-auth-oauthlib==1.2.3

# --- ユーティリティ ---
python-dotenv==1.2.1             # 旧 requirements では重複記載されていた → 1行に統合
requests==2.32.5

# =====================================================================
# 除外候補 (旧 requirements.txt に記載があるが、現行 .py で import 未検出)
# 必要になった時点で下記のコメントを外して有効化してください。
# ---------------------------------------------------------------------
# langchain-community            # import 0 箇所
# openai                         # 直接 import 0 箇所 (langchain-openai 経由で利用)
# fastapi==0.124.4               # import 0 箇所 (Web API を再導入するなら復活)
# uvicorn==0.38.0                # import 0 箇所 (fastapi とセット)
# line-bot-sdk                   # import 0 箇所 (LINE Bot を再導入するなら復活)
# =====================================================================
